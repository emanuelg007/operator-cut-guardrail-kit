export function openHeaderMapModal(headers: string[], previewRows: string[][]): void {
  // Stub only — real UI to be implemented later.
  console.log("Header Map Modal (stub):", { headers, preview: previewRows.slice(0, 3) });
}
